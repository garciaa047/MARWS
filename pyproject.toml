[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "marws"
version = "0.1.0"
description = "Multi-Agent Robotic Warehouse Simulation"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["simulation*", "training*", "scripts*"]
